const Loading = () => {
    return (
        <div className="flex flex-col content-evenly card p-6">
            <h2 className="text-sm font-semibold text-ink-soft">Loading...</h2>
        </div>
    );
};

export default Loading;
